---
topic: MyShadchan Phase 1 — a private shidduchim CRM: capture, triage, diligence and follow-through for a family, extended to multi-persona logins, single and shadchan participation, and opt-in discoverability
updated: 2026-07-26T03:44
---

- (event) Create from structured, pre-sorted upstream input (PRD v1 + Amendment A2 + decisions log D1-D22 + ARCHITECTURE-SPINE 24 ADs + epics.md). Rich input: extract directly, no elicitation.
- (direction) Daniel: adopt ARCHITECTURE-SPINE as a companion with AD IDs stable so epics.md and stories can cite them.
- (decision) PRD, Amendment A2, ARCHITECTURE-SPINE, SOLUTION-DESIGN and epics.md are ADOPTED companions (downstream reads them for FR/AD detail), not absorbed sources. addendum.md and decisions-log.md are sources: their load-bearing content is now carried by A2 and the spine.
- (decision) Capabilities are stated at epic altitude (CAP-1..12), not per-FR: 119 FRs are detail the adopted PRD/A2 companions carry.
- (decision) Spec-authored companions: glossary.md (shidduch vocabulary is unreadable without it) and personas-and-contexts.md (identity matrix + 6 family shapes is a multi-item catalog). The 360 tab matrix stays in AD-24 rather than duplicating into a companion.
- (capability) CAP-1 capture from any channel; CAP-2 one confirm step before filing; CAP-3 one canonical 7-state pipeline; CAP-4 recognise someone already seen; CAP-5 per-suggestion reference diligence; CAP-6 follow-through (reminders + assignable tasks); CAP-7 one consistent 360 view; CAP-8 several personas on one login; CAP-9 the single takes part; CAP-10 consent-based shadchan participation; CAP-11 communication across every party; CAP-12 opt-in discoverability + revocable sharing; CAP-13 AI that organises rather than judges.
- (event) SELF-VALIDATE PASS 1 (coherence, Spec Law 1-6,8): PASS. 13 capabilities, all with intent+success; 12 constraints each ruling something out; 8 non-goals; success signal demonstrable; no duplicate or reused CAP IDs; prose lean.
- (event) SELF-VALIDATE PASS 2 (preservation): PASS. D1-D22 all land (D1->CAP-7, D2/D3->CAP-9, D4->CAP-12, D5->CAP-10, D6/D7/D12/D14->CAP-8+personas companion, D8->CAP-12+constraint, D9/D15->CAP-11+constraint, D10/D13/D16->naming constraint, D17->epics companion, D18/D19->constraints, D20->personas companion). FR1-78 and FR79-119 carried by adopted PRD/A2 companions; 24 ADs by the adopted spine. Wrapper-only drops (not preserved, on the record): PRD narrative backstory, review rubrics, upstream memlogs, process metadata.
- (assumption) Capability count is 13 (an earlier memlog line said CAP-1..12 before the AI capability was split out); CAP IDs 1-13 are now fixed and must not be renumbered.
- (event) spec finalized
