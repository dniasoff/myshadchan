---
scope: MyShadchan v1 whole-product architecture; brownfield fork of Atomic CRM on Vercel+Supabase+Upstash; keeps the 11 build epics coherent.
purpose: build-substrate spine + full solution-design doc
altitude: initiative (governs MyShadchan v1; the level below is the 11 epics)
updated: 2026-07-21T22:48
---

- (direction) Host/compute = Vercel (user pre-set). Repo is a Vite SPA (vercel.json forces Vite framework), so server-side work runs as Vercel Functions in /api (Fluid Compute, Node) + Vercel Cron, NOT Next.js routes.
- (direction) Data = Supabase (Postgres + RLS + Auth + Storage; user pre-set). Redis + queues = Upstash (user pre-set) -> caching, rate-limiting, async parse/reminder job queue (NFR-9 cost lever).
- (direction) CI/CD = GitHub Actions -> Vercel (user pre-set). Repo already has .github/workflows/check.yml + deploy.yml.
- (direction) Scaffold = Atomic CRM fork = THIS repo (user pre-set, said 'atlas crm' -> read as Atomic CRM, ASSUMPTION to confirm). Schema still vanilla: companies/contacts/deals/sales/tags/tasks; single-team 'sales' model, no account tenancy yet.
- (direction) AI access = Cloudflare AI Gateway (user pre-set). Unified proxy for LLM/vision providers: caching + rate-limit + provider fallback. All AI (OCR-extraction, reference-question gen, cross-ref summaries) routes through it. Supersedes Vercel-AI-Gateway default.
- (direction) AI observability/tracing = Langfuse (user pre-set). LLM traces, cost tracking, prompt mgmt, evals -> serves NFR-5 auto-parse reliability + NFR-9 cost.
- (direction) Product analytics = PostHog (user pre-set). Powers section-17 metrics: filing/pipeline events, dedupe-confirm events, referrals/invite tracking, child felt-surveilled sentiment check.
- (question) OCR path undecided: vision-capable LLM via Cloudflare AI Gateway (one call OCR+extract, cacheable) vs dedicated OCR (Textract/Document AI) then LLM extract. Addendum leaned Textract/DocAI; gateway direction favors vision-LLM. Resolve in draft as ASSUMPTION.
- (decision) 'atlas crm' CONFIRMED by user = Atomic CRM (the existing fork). Assumption resolved.
- (constraint) Vanilla RLS = 'to authenticated using(true)' on EVERY table (05_policies.sql) -> NO tenant isolation; single shared workspace. PRV-3 is 100% net-new: every domain table needs account_id (tenant) + membership-based RLS. This is the core AD and the biggest lift.
- (constraint) sales table = user record (1:1 auth.users via user_id, has administrator bool). Trigger set_sales_id_default() stamps ownership on insert but does NOT isolate. handle_new_user/handle_update_user sync auth.users->sales (04_triggers). This user<->auth sync pattern is ratifiable and extends to the account/membership/role model.
- (constraint) Storage = single 'attachments' bucket, policy any-authenticated read/write/delete (07_storage). No tenant scoping, no private bucket, no signed/expiring/access-logged links. PRV-5/PRV-8/PRV-10 (access-controlled storage, revocable per-recipient expiring links, field-encryption for photos/health) all net-new.
- (constraint) Auth = Supabase (config.toml): password + OAuth + SMS-OTP scaffolding, enable_confirmations=false, asymmetric signing keys present, Twilio SMS provider stub (disabled). No magic-link/passkey UI yet; passwordless PRV-9 is native-supported config+UI change on Supabase. Views use security_invoker=on (RLS flows through views) - ratify.
- (decision) Existing 'postmark' Supabase Edge Function (Deno) already: parses inbound + FORWARDED email, recovers original sender, extracts+uploads attachments, adds note to contact. Big head-start for FR22-26 email channel. Raises the compute-home AD: existing Deno edge functions vs pre-set Vercel Functions(Node)+Upstash. Lean = Vercel Functions primary for new pipeline (webhooks/AI-orchestration/cron/share-render); Supabase = data plane; port/keep postmark. Resolve as ASSUMPTION in draft.
- (direction) Free observability: user OK with 'just PostHog if enough'. Decide: PostHog free tier = product analytics + error tracking + session replay + SURVEYS (covers PRV-4 felt-surveilled check) as the app-observability home; Vercel-native = function/deploy logs+metrics; Langfuse = AI traces. No separate Sentry unless wanted (note as optional).
- (decision) COMPUTE-HOME AD (recommendation, user deferred 'cloudflare workers for ai tasks IF recommended'): keep ONE app-compute home = Vercel Functions (Node/Fluid, 300s) + Upstash QStash for durable async parse/reminder jobs. AI tasks = Vercel Functions calling Cloudflare AI Gateway. DEFER Cloudflare Workers for v1 (avoids 3rd runtime/split-brain; AI Gateway already delivers CF caching+fallback+analytics without moving compute). Revisit triggers: Workers AI models, R2 co-location, edge-latency-critical inference.
- (constraint) Current role model = single is_admin() (sales.administrator for auth.uid()). handle_new_user() makes FIRST signup admin, rest non-admin, auto-creates sales row. This bootstrap is single-tenant; multi-tenant needs invite/membership-based account join instead. RBAC parent/child/helper/self-manager(+Phase2 shadchan) is net-new on top of the sales<->auth pattern.
- (decision) Reusable assets found: merge_contacts() SQL (dedupe emails/phones/tags, reassign refs) = pattern for FR20 reference-contact dedup (but suggestions NEVER auto-merge - surface+confirm). postmark fn CC+FORWARD handling, original-sender recovery, IP-allowlist+Basic-auth, attachment extract/upload = most of FR22-25 (currently matches sender vs sales emails; must re-point to accounts + inbox_items). Ratify parsing code, re-target tenancy.
- (reference) Repo rules to RATIFY as Consistency Conventions (no re-deciding): coding-style.md (immutability, camelCase/PascalCase/UPPER_SNAKE, files 200-400 typ/800 max, KISS/DRY/YAGNI), typescript.md (no any; unknown+narrow; Zod for validation + infer types; interface for shapes, type for unions), web-patterns.md (TanStack Query=server state, URL=shareable state, RHF=forms; API envelope {success,data,error,meta}), web-security.md (HSTS/nosniff/XFO/Referrer/Permissions headers + nonce CSP; CSRF+rate-limit on state-changing endpoints), english-only.md, testing.md (80% cov, AAA, Playwright deterministic waits), lsp-usage, security-triggers.
- (decision) Sentry = DEFERRED to Phase 2 (v2), bundled with the shadchan-interface milestone (user: 'sentry v2 together with shadchan'). v1 error tracking stays on PostHog. Observability planes v1: PostHog (product analytics + errors + session replay + surveys) | Vercel-native (function/deploy logs+metrics) | Langfuse (AI traces). Phase 2 adds Sentry. Stack now LOCKED (user 'all good').
- (constraint) SCAN: Auth password-only (signInWithPassword + Google SSO). ra-supabase-core@3.5.2 has NO OTP/magic-link/passkey -> passwordless PRV-9 = from-scratch build. Single-tenant isInitialized gate limits signup to FIRST user only (authProvider.ts:134, SignupPage.tsx:93) -> must be replaced by invite/membership-based signup. Role model today = is_admin() only.
- (constraint) SCAN: dataProvider = supabaseDataProvider -> custom-method overlay (getList view-routing + 9 non-CRUD methods, dataProvider.ts:47) -> withLifecycleCallbacks (ResourceCallbacks[], :260). TWO extension seams to ratify. companies/contacts reads redirect to *_summary views (need same treatment for new list resources). Provider selection is BUILD-TIME (main.tsx=supabase, demo/main.tsx=fakerest) -> fakerest provider must stay in sync per new resource/method or demo build breaks. Desktop+Mobile register DIFFERENT resource sets via useIsMobile().
- (constraint) SCAN: PWA ~70% wired (vite-plugin-pwa 1.2 autoUpdate + Workbox SW + maskable icons + standalone manifest.json). MISSING: manifest share_target (FR2 Android 1-tap) AND persisted offline mutation/outbox queue (NFR-3 offline capture) - only mobile read-tolerance today. Deploy = TWO independent tracks: Vercel Git-integration (SPA static, dist, SPA rewrite) + GH-Actions deploy.yml deploys ONLY Supabase (db push + functions + secrets). Upstash = declared-but-UNUSED (secrets only; no client code). Node .nvmrc 24.18.0.
- (decision) SCAN reusable: 6 edge functions incl. postmark (inbound/forwarded email), users (admin user mgmt), merge_contacts (kysely), delete_note_attachments (storage cleanup webhook), mcp (MCP server, JWT+OAuth, direct Postgres). Existing Supabase-as-OAuth-server consent flow wired in authProvider (approve/denyAuthorization + oauth-consent-page). Adaptation ~0% in src/ ('Atomic CRM' x33, stock resources/taxonomies/logos). update-branding skill exists.
- (reconsider) COMPUTE-HOME AD REOPENED by user cost push ('Cloudflare Workers cheaper than Vercel'). Valid for FREE product. Real lever = R2 zero-egress (resume PDFs/photos + repeated share-link serving FR47-49) + Workers cheap per-request + co-location w/ AI Gateway. Workers compat fine for our workload (fetch + supabase-js + R2 uploads + AI-gateway calls; OCR=vision-LLM so no heavy native libs). Options: A Cloudflare-first (Workers+R2+Queues; SPA static on Vercel; Supabase data plane) [new lean] | B Vercel Functions+Upstash | C hybrid (Vercel app + Workers AI + R2). Asking user 1 crisp fork; 'latest versions, official sources' -> version-verify agent dispatched.
- (decision) COMPUTE-HOME AD RESOLVED (user: 'vercel expensive for everything other than website' + 'am i correct' -> confirmed). Option A Cloudflare-first LOCKED: Vercel = website/SPA static host ONLY (free, existing Git integration). ALL server compute = Cloudflare Workers (email/SMS webhooks, REST API, AI orchestration, share-link render, cron). Media/files = Cloudflare R2 (zero egress) fronted by a Worker minting signed+expiring+access-logged URLs (PRV-5/8/10). Async+cache = Upstash Redis/QStash (pre-set, edge-native from Workers; kept to avoid churn; CF Queues/KV noted as consolidation option in Deferred). Data/Auth/RLS = Supabase. AI = Cloudflare AI Gateway called from Workers + Langfuse. Rationale: R2 zero-egress is the dominant saving for media/share-heavy FREE product (NFR-9/R4); Workers cheaper per-req + co-located w/ AI Gateway.
- (decision) DEPLOY/COMPUTE consequences of Option A: (1) NEW deploy track = Cloudflare Workers via wrangler (add to GH Actions alongside existing Supabase track; Vercel stays Git-integration for SPA) -> 3 deploy surfaces. (2) Existing Supabase Edge Functions (Deno): keep for DB-trigger-adjacent tasks (delete_note_attachments webhook, mcp server); ingestion webhooks (email/SMS) BUILT/PORTED as Workers reusing postmark parse logic. Rule: primary app-compute = Workers; Supabase functions only for DB-adjacent. (3) Supabase Storage 'attachments' bucket -> migrate served media to R2; small incidental attachments may stay. ALL tagged [ASSUMPTION] for Fast-path review.
- (direction) Version pinning: user 'use latest for everything' + 'check official sources'. Background agent dispatched to verify latest STABLE (no beta/rc) from official sources (npmjs, official docs, nodejs.org, developers.cloudflare.com) as of 2026-07-21 for full dep + toolchain + new-integration SDK list; flag major bumps vs pinned. Stack table pins verified-latest, NOT current package.json.
- (version) Pinned latest-stable (official sources, 2026-07-21): Node 24.18.0 LTS | React/react-dom 19.2.8 | Vite 8.1.5 + plugin-react 6.0.3 | TypeScript 7.0.2 (tsgo) | react-router 8.2.0 | ra-core/ra-data-fakerest 5.15.0 | ra-supabase-core 3.5.2 | Tailwind 4.3.3 | TanStack Query 5.101.4 | RHF 7.82.0 | Zod 4.4.3 | vite-plugin-pwa 1.3.0 | @supabase/supabase-js 2.110.8 (stay 2.x) | supabase CLI 2.109.1 | wrangler 4.113.0 | @cloudflare/workers-types 5.20260721.1 | Upstash redis 1.38.0/qstash 2.11.2 | @anthropic-ai/sdk 0.112.4 | ai 7.0.34 | @langfuse/tracing+otel 5.9.1 | posthog-js 1.406.1/node 5.46.0 | twilio 6.0.2 | postmark 5.1.0 / resend 6.18.0.
- (constraint) VERSION RISK REGISTER (latest != always safe). #1 TypeScript 7.0.2 = Go-native tsgo rewrite GA 2026-07-08 (2 wks old); semantics = TS6 but full compiler port -> validate CI/tooling before pinning; 5.8.3 fallback. Vite 8 + plugin-react 6 move together (hold Vite 7.3.6). react-router 8 (hold 7.18.1). ESLint 10 (flat-config only, min-Node). shadcn 4 (dev-only CLI). Storybook 10 (upgrade automigrations). marked 18 + lucide-react 1 util majors. @supabase/supabase-js STAY 2.x (v3 prerelease). Node stay 24 LTS (26 not LTS until Oct 2026).
- (decision) Cloudflare mechanisms (official docs, confirmed): compatibility_date = today (2026-07-21); nodejs_compat GA (needs date>=2024-09-23). R2/Queues/KV/AI-Gateway all GA. Access = Worker BINDINGS ([[r2_buckets]] env.BUCKET, [[queues.producers/consumers]] env.QUEUE.send()+queue() handler, [[kv_namespaces]] env.KV). AI Gateway from Worker = point provider SDK baseURL at https://gateway.ai.cloudflare.com/v1/{acct}/{gw}/{provider} (NOT the Vercel-AI-SDK 'gateway' feature which = Vercel AI Gateway). Langfuse = @langfuse/tracing + @langfuse/otel 5.9.1 (OTel rewrite), NOT legacy monolithic langfuse 3.x.
- (event) Reviewer gate: lint_spine clean (0). versions-reality: no crit/high, 2 mediums APPLIED (passkey=Beta so magic-link/OTP is native load-bearing path + passkey progressive-enhancement; OCR vision-LLM Hebrew-handwriting/hallucination risk named; TS7 GA-date softened).
- (constraint) SECURITY REVIEW findings TO APPLY (review-security-tenancy.md). F1[crit] AD-5: matching MUST be account-scoped (every identity_signals query filtered by account_id) - unscoped recall = cross-account leak + breaks PRV-2 no-pooling in the signature feature. F2[crit] AD-7: service-role seam needs a MECHANISM not a rule -> mandatory scoped-client helper forAccount(accountId) as the ONLY way Workers touch tenant tables (raw access unrepresentable); postmark:55 + users:191 prove omission is easy. F3[high] AD-3: dignity enforced at suggestion grain but candid content lives on reference_links/interactions/resumes w/ no visibility col -> child hitting base tables via PostgREST sees candid diligence (breaks FR68/PRV-4); extend visibility to child tables via join-to-parent RLS.
- (constraint) SECURITY cont'd: F4[high] AD-9: 'revoke=immediate'+access-log INCOMPATIBLE with pre-signed R2 URLs -> mandate Worker-PROXIED STREAM (check revoked/expired + log every request); recipients never get a raw R2 URL. F5[high] AD-6: forwarded-email port is a BEHAVIOR CHANGE not lift-and-shift (postmark:69-84 silently attributes to first body email = cross-account write); deterministic sender->account, ambiguous->unattributed+flag. F6[high CONFIRMED 06_grants:69-107,174-177] AD-1: anon has grant-all on every table + default-privilege auto-grants FUTURE tables -> REVOKE anon grants + drop anon default-priv + CI assert every public table rowsecurity=true (FORCE RLS). F7[high] AD-11/2: invite must bind inviter's account_id + role<=authority + no role mass-assignment + membership only via verified invite token (not email match).
- (constraint) SECURITY mediums: F8 AD-2 reserved shadchan role = granted NOTHING in v1 RLS (deny posture, not placeholder access). F9 AD-1 polymorphic interactions/tasks need account-integrity (composite (account_id,id) FK or trigger; reads filter own account_id). F10 AD-1/3 curated views security_invoker=on; DROP init_state (definer view, currently anon-granted); CI test no public view is definer. F11 AD-8 AI-gateway cache namespace by account (existence/timing oracle) + document field-enc scope (PDF embeds photo/health in clear in R2/SSE). F12 merge logic account-scope (covered by F2).
- (constraint) BROWNFIELD/RUBRIC findings TO APPLY (review-rubric-brownfield.md). [high] NEW AD needed: data export/delete/retention/per-single-purge (PRV-2/PRV-11/NFR-8/NFR-10 = the wedge) has NO home + CONTRADICTS fork 'user deletion not supported' (AGENTS.md, sales.disabled) -> add AD-15 data-lifecycle. [high] Operational envelope AD-less + frontmatter companions:[] doesn't bind SOLUTION-DESIGN -> fix companions + add ops (backup/DR, retention, Worker error-monitoring: v1=Cloudflare Workers native observability/Logs/Tail/Logpush since Sentry is Phase2 + PostHog=product not backend errors). [med] AD-3 visibility.ts is TS but both enforcement points are SQL -> single SQL source of truth (SECURITY DEFINER fn) used by RLS+views; TS is client mirror only. [low/med] Email is NOT either/or: fork runs Postmark INBOUND + Resend OUTBOUND (deploy.yml:23,54) -> Stack fix, resolves Open-Q#3.
- (question) VERSION POSTURE now reviewer-flagged too (rubric [med]): pinning 6 simultaneous majors (TS7/tsgo, Vite8, rr8, ESLint10, SB10, shadcn4) in a BINDING spine is risky; reviewer recommends defaulting to the hold/stable column (all already installed, CI-green). Tension w/ user 'use latest for everything'. SURFACE to user: recommend default-stable + latest-majors as tracked fast-follow; user decides.
- (event) Reviewer gate reconciled + APPLIED (all 4 lenses). Spine rewritten: added single-owner cross-cutting rule; AD-1 (+FORCE RLS, revoke anon grants, CI RLS check, polymorphic integrity, no definer views); AD-2 (shadchan deny-only); AD-3 (one SQL child_visible fn, exhaustive 7-state, child-table visibility); AD-4 (collapse to one pipeline_state, one createSuggestion INSERT gate in foundation, one transitionSuggestion guard); AD-5 (account-scoped + one Postgres normalize trigger, references dedup same service); AD-6 (deterministic non-cross-account attribution, email behavior-change, channel-untrusted->inbox only); AD-7 (forAccount scoped client mechanism + trusted-root ranking); AD-8 (account-namespaced cache); AD-9 (Worker-proxied stream, no raw/pre-signed URL); AD-11 (verified-invite binding, no role mass-assign); AD-12 (normalize=AD-5 fn); AD-13 (email floor); NEW AD-15 (export/delete/purge lifecycle). Stack: email=Postmark-in+Resend-out; companions bound; ops section added. SOLUTION-DESIGN rewritten to match. Open: version posture (user decision), OCR/encryption/Hono assumptions.
- (decision) Version posture RESOLVED (user chose Stable-default): pin risky majors to CI-green hold lines (Vite 7.3.6+plugin 4.6.x, TS 5.8.3, react-router 7.18.1, ESLint 9.22, Storybook 9.1.20, shadcn 3.5, marked 17, lucide 0.542); verified-latest = tracked fast-follow adopted one-at-a-time behind green CI. Non-major rows stay verified-latest. TO APPLY to Stack rows + open-Q.
- (direction) NEW REQUIREMENT (user): ALL tools must have a generous FREE tier, except AI models (cost unavoidable, acknowledged). Reopens SPA-host + email-inbound. Preliminary audit -> recommend 2 swaps that also consolidate on Cloudflare: (1) SPA host Vercel Hobby -> Cloudflare Pages (truly free, unlimited bw, no non-commercial clause). (2) Email inbound Postmark(free tier ~100/mo too small) -> Cloudflare Email Routing + Email Worker (free); outbound stays Resend (free 3k/mo). Keep Upstash (free) over Cloudflare Queues (needs Workers Paid $5). Caveats: Twilio SMS = no free tier but OPTIONAL (email is the AD-13 floor) -> defer for free v1; Supabase free tier OK for MVP but pauses after 7d inactivity + 500MB DB (Pro $25 at scale); Workers free 100k/day (Queues/DO/paid features -> $5 at scale). AI Gateway itself free; only model tokens cost. Dispatched agent to verify limits vs official pricing pages.
- (decision) Pricing = FREEMIUM (user chose). Free core preserves PRD free-community wedge (R6); $2/mo premium tier. Premium scope TBD (candidates: AI research assistant, >1 child, SMS capture, higher AI/parse limits). Architecture = per-feature entitlement gating + a defined free tier, driven by AD-16 synced subscription state. Annual $24/yr option recommended (Stripe 30c fixed fee = ~18% on $2 monthly, ~4% annual).
- (decision) Email inbound: user leaning RESEND INBOUND (already Resend outbound). Consolidates to ONE provider + parsed webhooks (vs raw-MIME in CF Email Worker). Maturity slightly behind Postmark for tricky forwarded parsing, but AD-6 makes US own sender->account recovery regardless, so provider parsing quality is not load-bearing. Cheaper-vs-CF-Email-Routing(free/unlimited) = wash at low volume; depends on Resend inbound free volume (verifying). LEAN: Resend in+out; CF Email Routing = free/unlimited fallback at scale. Postmark DROPPED (free tier ~100/mo too small).
- (direction) SMS scope CHANGE (user): NO outbound SMS ever. INBOUND-only capture via 3 shared country numbers: US, UK, Israel; sender-lookup routing by verified account_phone (FR29). Updates: AD-13 delivery = in-app + email(floor) + push (NO SMS); AD-6/FR28-30 = inbound-only, drop outbound. FLAGS: (a) Israel Twilio SMS-number availability + regulatory = the risk (verify; Telnyx/local alt); (b) MMS image-capture is mostly US (UK/IL SMS=text; images via WhatsApp/email) -> SMS-image channel is US-centric; (c) UK+Israel users REOPEN jurisdiction: PRD is US-only/US-compliance (superseded UK/GDPR) but UK/IL users bring UK-GDPR/EU-GDPR/Israeli privacy into scope -> FLAG legal + PRD update (AD-1/AD-15 cover mechanics). Fixed cost ~$3-6/mo for 3 inbound numbers.
- (constraint) PRD DIVERGENCES accumulating (offer PRD update at finalize): (1) 'Free, no monetisation' -> $2/mo freemium; (2) 'US-only jurisdiction, supersedes UK/GDPR' -> UK+Israel inbound numbers/users reopen UK/EU/Israel privacy law. Both are product-positioning changes the architecture now supports; upstream PRD should be reconciled so it doesn't silently disagree with the spine.
- (constraint) FREE-TIER VERIFIED (agent, official sources 2026-07-21). Generous/free: all Cloudflare (Pages unmetered bw; Workers 100k/day+10ms CPU; R2 10GB+FREE egress; Queues NOW FREE since 2026-02, 10k ops/day+24h retention; Cron 5/acct; Email Routing UNLIMITED inbound + free Email Workers; AI Gateway free+100k logs; KV 100k reads but only 1k WRITES/day); PostHog (1M events+5k replays+surveys, hard-stop no surprise bill); GitHub Actions UNLIMITED free on PUBLIC repo; Langfuse self-host MIT free / cloud 50k units/mo. WILL HIT PAID: Supabase (500MB DB+50k MAU BUT 7-day inactivity PAUSE -> Pro $25/mo for always-on = FIRST real cost); Resend (3k/mo AND 100/DAY hard cap - tight for magic-link+reminders+inbound combined); Upstash (Redis 500k cmds/mo, QStash 1k msgs/day). PAID: Twilio ~$1.15/mo/number x3 (no free tier); Postmark INBOUND no free (Pro ~$16.50). OUT: Vercel Hobby - clause verified 'non-commercial personal use only', DONATIONS + paid-dev = commercial -> $2 cost-recovery disqualifies -> Cloudflare Pages SETTLED. REFINEMENTS: AD-17 rate-limit = Upstash Redis NOT CF KV (1k writes/day); CF Queues free = consolidation option (keep Upstash, needed for rate-limit Redis); recommend PUBLIC repo for free CI; Langfuse self-host option. COST FLOOR always-on ~$28/mo (Supabase Pro 25 + 3 numbers 3.5) + AI tokens; $2 x ~15 families covers it.
- (decision) EMAIL RESOLVED (agent-verified): inbound = Cloudflare Email Routing -> Email Worker (FREE, UNLIMITED inbound). NOT Resend inbound - Resend counts inbound against the SHARED outbound quota (3k/mo + 100/day), which email-as-primary-capture+login would exhaust. Outbound (magic-link, reminders) = Resend (free 3k/mo,100/day; Pro $20 later). Email Worker parses raw MIME (postal-mime); we own sender-recovery per AD-6 anyway. Postmark fully dropped.
- (decision) SMS NUMBERS RESOLVED (agent-verified): provider = TELNYX (recommended) for all 3 inbound-only numbers US/UK/Israel. Israel is the catch: regulatory bundle (ID+address), ~$1/mo Telnyx vs ~$15/mo Twilio (same paperwork); US/UK ~$1/mo. INBOUND MMS = US/Canada ONLY -> US number is the only real picture-capture line; UK/Israel = text SMS only (images via WhatsApp/email). Twilio = mature fallback for US/UK. No outbound SMS. ~$3/mo fixed (Telnyx).
- (decision) OCR Hebrew (user: 'vision-LLM weaker on Hebrew handwriting - need hebrew supporting alternative'). Direction: NOT vision-LLM-alone. PRIMARY = Hebrew-strong vision model (likely GEMINI - strong Hebrew) via AI Gateway (keeps AD-8, gateway supports Google Vertex/AI-Studio). FALLBACK = dedicated Hebrew OCR (Google Document AI / Cloud Vision, or Azure Read) for hard/handwritten (direct disclosed sub-processor call = noted AD-8 exception). Textract DROPPED (weak Hebrew). Reality: most shidduch resumes are TYPED Heb+Eng -> well-handled; handwriting = rare edge, human-review backstop (AD-6/NFR-5). Agent verifying best Hebrew OCR -> pin AD-8/Stack.
- (direction) MMS: user 'nobody really uses mms' -> reinforces SMS TEXT-only (Telnyx, no MMS). SMS = minor text-capture lane (name/note/link); resume IMAGES via email + WhatsApp-share + upload (the workhorses).
- (direction) NEW REQ (user): multi-language UI + browser-lang detect, English+Hebrew to start, extensible. Added AD-18 (UI i18n + BIDIRECTIONAL RTL/LTR) + convention row. Ratifies fork ra-core i18nProvider (polyglot, ships en+fr); add Hebrew catalog + navigator.language detect (default en, persisted override) + domain-string translations. FLAG: Hebrew=RTL -> whole UI must be bidi (CSS logical props ms/me not ml/mr; root dir per locale; Radix/shadcn dir; RTL testing); fork LTR-only today = real cross-cutting audit. DISTINCT from AD-12 (data-bilingual-for-matching). [ASSUMPTION] ra-language-hebrew + authored domain strings. Also updated: Open-Q5 (annual+bank-debit), Resolved line (SMS text-only no-MMS). OCR re-request = already-running agent (will report, not re-dispatched).
- (decision) OCR PINNED (agent-verified, official docs). Typed Hebrew+English = SOLVED. PRIMARY = GEMINI (Flash default -> Pro hard) via Cloudflare AI Gateway (Google Vertex/AI-Studio = native gateway providers) -> structured JSON, ~$0.001-0.003/typed page; keeps AD-8. Only strong Hebrew reader that is ALSO gateway-native (Vision/DocAI/Azure do typed Hebrew but NOT handwriting + are NOT gateway-proxyable; Textract = NO Hebrew, dropped). DETERMINISTIC fallback (degraded typed) = Google Document AI/Cloud Vision (DIRECT disclosed Google Cloud call = AD-8 exception). HANDWRITTEN Hebrew (rare, no cloud API) = Gemini-with-verification or Transkribus/Kraken HTR (deferred edge). All generative OCR hallucinates -> guard = field validation + low-confidence human review (AD-6/NFR-5). CAVEAT: vendors publish lang-support not accuracy -> validate Gemini on sample resumes. Pinned AD-8 + Stack; Open-Q resolved.
- (event) Spine finalized (status:final). 18 ADs; gate-hardened (4 lenses, criticals+highs applied); stack + free-tiers + Hebrew-OCR + phone/email options all official-source verified. SOLUTION-DESIGN reconciled to match. Open items (tagged, non-blocking): field-encryption, Hono, freemium-split, billing-provider; validate Gemini OCR on sample resumes. PRD divergences flagged (monetisation, UK/Israel jurisdiction, new AD-16/17/18 + SMS-text-only) -> offer bmad-prd update. Next: bmad-create-epics-and-stories.
