---
baseline_commit: bad63baa8c615c9f43067549e3647a1eaba53f98
---

# Story 2.7: Invite-Only Signup with 18+ Affirmation

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a platform owner,
I want new members to join only by a verified invite, with an affirmed age,
so that every connection into the product is consent-based and safe by construction —
never an open door.

## Dependencies

**Requires Story 2.6** (the OTP primitives this story's accept flow calls — this story
adds invite verification and 18+ affirmation *around* email-OTP, it does not build a
second signup mechanism). **Requires Story 2.1** (`current_context_id()` — `invites` is
RLS-scoped by it; 2.1 *deletes* `current_account_id()` outright, so this story must be
written against the new name and cannot land before it) and **Story 2.2**
(`account_members_role_check` including `single`, `accounts.kind`, and
`enforce_membership_role_matches_context()`, which the invite-acceptance binding step
relies on to keep a `shadchan`-role invite from ever landing on a household).
**Feeds Story 2.8**, which adds the inviter-side UI on top of the `invites` table and
`create_invite()` function this story builds.

**Baseline note (Epic 1 has shipped).** This story was first drafted before Epic 1. The
tree it now targets has `members` (not `sales`), `singles` (not `children`),
`single_id`/`member_id` (not `child_id`/`sales_id`), no fork fossils (contacts,
companies, deals, deal_notes, contact_notes, tags, favicons_excluded_domains), no token
portal, and — critically for AC-8 — **no hand-written `<Route>`/`<Resource>` JSX**:
route and resource registration is the manifest at
`src/components/atomic-crm/root/routeManifest.ts`, mapped over by `DesktopAdmin` /
`MobileAdmin` in `root/CRM.tsx`. Every path, line number and count below was
re-verified against that tree.

## Scope boundary with Story 2.8 — read this first

This story builds the **mechanism** and the **invitee's** acceptance experience:
the `invites` table, `create_invite()` (the function; its calling UI is 2.8's),
`handle_new_user()`'s rewritten binding logic, the signup-gating Auth Hook, and
`login/InviteAcceptance.tsx` rewritten for OTP + the 18+ affirmation. It also retires
`is_admin()` (AC-9) — the sibling of `isInitialized` in AD-2's directive, deleted in
the same story because both are the fork's "first user is special" world ending at
once. Story 2.8 builds the **inviter's** side: the Settings UI to send an invite, the
invite list, and `revoke_invite()` — and deletes the fork's parallel admin-invite
path. Do not build an invite-sending UI here — there is nothing yet for a real user to
click that creates one (2.7's own tests seed invite rows directly in SQL); that is
2.8's job.

## Decision — how the very first user, ever, gets in

Every invite in this design requires an `invited_by` — "an existing member," per the
epic's own AC text. That is correct for every household/shadchanus invite a real user
sends, but it leaves the founding case unaddressed: nobody exists yet to invite the very
first person. **Decision:** the platform owner seeds the first household and its first
invite directly via SQL against the production database (two `insert` statements — one
`accounts` row, one `invites` row with `invited_by = null`) — an operational runbook
action, not an in-app flow. `invites.invited_by` is nullable specifically to allow this
one seeded row; RLS on `invites` still requires `service_role` for any insert with
`invited_by is null` (AC-2), so the app itself never exposes a way to create one. This
keeps FR119 ("invites are the one mechanism for household membership") literally true —
even the founder's own membership arrives via an invite row, just one minted by hand
rather than by another member — while asking this story to build nothing beyond what
its own ACs call for. Flagged explicitly rather than silently assumed, since it is this
story's own resolution of a gap epics.md leaves open.

This decision replaces `handle_new_user()`'s current first-user bootstrap
(`02_functions.sql:57-70`), which is what makes AC-7's deletion safe rather than
merely destructive.

## Acceptance Criteria

1. **`public.invites` is the one table every invite — genesis or otherwise — lives in.**
   Declared in `supabase/schemas/01_tables.sql` in the file's house style: columns
   inline, **foreign keys in the file's separate `alter table … add constraint …_fkey`
   section** (`01_tables.sql:491-618`, where every other FK in this schema lives — no
   inline `references`), and the FK-backing indexes in the "Indexes on foreign keys"
   section below it.
   Columns: `id bigint generated by default as identity primary key, created_at
   timestamptz not null default now(), token uuid not null default gen_random_uuid()
   unique, email text not null, account_id bigint not null, role text not null check
   (role in ('parent_admin', 'helper', 'single', 'shadchan')), invited_by bigint,
   status text not null default 'pending' check (status in ('pending', 'accepted',
   'revoked', 'expired')), expires_at timestamptz not null default (now() + interval
   '14 days'), accepted_at timestamptz`. Constraints:
   `invites_account_id_fkey` → `public.accounts(id) on delete cascade`;
   `invites_invited_by_fkey` → `public.account_members(id) on delete set null`.
   `self_manager` is deliberately **not** an invitable role (Dev Notes
   "Why `self_manager` is never invited"). Decided by: `01_tables.sql` diff plus
   `\d public.invites` in psql.

2. **RLS: a context's own active members see its own invites; nothing writes the table
   except this story's functions and `service_role`.** One **select-only** policy for
   `authenticated`: `using (account_id = public.current_context_id())`. There is **no**
   insert/update/delete policy and **no** DML grant to `authenticated` at all — every
   write goes through `create_invite()` (AC-3), `revoke_invite()` (Story 2.8),
   `handle_new_user()`'s binding step (AC-7, a definer trigger), or the `service_role`
   genesis seed. This is deliberate, not a shortcut: if `authenticated` could insert
   with only an account-match `with check`, all of AC-3's authority/kind checks would
   be advisory — any active member, including a `helper`, could PostgREST-insert an
   invite row with `role = 'parent_admin'` and a hand-picked `invited_by`, read the
   token back through the select policy, and mint themselves (or anyone) an admin
   membership. The authority boundary must hold at the grant, not just inside the
   function. Negative tests (AC-10): a direct `insert` into `invites` as
   `authenticated` is refused (both with and without `invited_by`), and a direct
   `update` (e.g. flipping `status` or `role`) is refused likewise; the
   `invited_by is null` genesis insert succeeds only as `service_role`.
   `alter table public.invites enable row level security;` **only** — not `force`: no
   table in `supabase/schemas/` carries `force row level security` today (verified: zero
   hits), and Story 2.1 explicitly declines the repo-wide FORCE pass and re-flags it as
   an unassigned AD-1 gap. `invites` matches its neighbours rather than becoming the one
   exception.

3. **`public.create_invite(p_email text, p_role text) returns public.invites` is the
   one function that creates an invitee-facing invite.** `SECURITY DEFINER`,
   `SET search_path = ''` (the `handle_new_user()` precedent, `02_functions.sql:25-27`):
   AC-2 withholds every DML grant on `invites` from `authenticated`, so an
   invoker-rights insert would be refused at the grant before its own checks ever ran —
   the definer mode is the consequence of closing the direct-write escalation, and the
   function's own explicit checks are therefore the *only* write gate and must all be
   present. It starts with an explicit active-membership check on
   `current_context_id()`
   (`exists (select 1 from public.account_members am where am.account_id =
   public.current_context_id() and am.user_id = auth.uid() and am.status = 'active')`),
   then raises unless: the caller holds an **owning** role
   (`parent_admin`, `self_manager` or `shadchan`) in the active context — a `helper` or
   `single`-role member may never invite anyone; and `role_authority(p_role) <=
   role_authority(caller's own role)` (`public.role_authority()`, a small `IMMUTABLE`
   helper: `parent_admin = 3, self_manager = 2, helper = 1, single = 1, shadchan = 1`).
   This is the concrete shape of "role ≤ inviter authority" from the epic text. **It
   also raises on a role/context-kind mismatch**: on a `household`-kind active context
   (`accounts.kind`, added by Story 2.2), `p_role` must be in (`parent_admin`,
   `helper`, `single`); on a `shadchanus`-kind one, `p_role` must be `shadchan`.
   Without this check, a shadchan could mint a `helper` invite (authority 1 ≤ 1 passes)
   that 2.2's `enforce_membership_role_matches_context()` trigger then rejects at
   acceptance — an invite that can never succeed must be refused at creation, not
   discovered broken by the invitee. Note this invite-capable list (`parent_admin`,
   `self_manager`, `shadchan`) is deliberately **broader** than 2.2's owning-role helper
   (`parent_admin`, `self_manager`) — a shadchan can invite into their shadchanus but
   never owns a `singles` row; do not merge the two predicates. Sets
   `invited_by` from the caller's own `account_members.id`, never from a parameter.

4. **The invitee accepts by completing OTP signup with the invite's email — there is no
   separate password/acceptance form.** `login/InviteAcceptance.tsx` (rewritten from its
   current 153-line password-based shell, which has **zero importers** today — it is
   registered on no route and rendered by nothing): reached at `/accept-invite/:token`,
   it looks up the invite by token (a `get_invite_preview(p_token uuid)`
   `SECURITY DEFINER` function returning only `email`, `account_name`, `role`, `status`,
   `expires_at` — enough to render "You've been invited to join The Klein Family as a
   helper," never the inviting account's actual data), shows the AD-11 18+ affirmation
   checkbox (`login/AgeAffirmation.tsx`, 118 lines, likewise imported by **nothing**
   today — this story is its first real wiring *and* its first import), and — once both
   the invite is valid and the box is checked — calls Story 2.6's
   `login({ email: invite.email, requestOtp: true, allowSignup: true,
   meta: { invite_token, age_affirmed: true } })` — `allowSignup`/`meta` are the seam
   2.6 AC-2 built for exactly this call, and this flow is the **only** caller in the
   product that passes `allowSignup: true` (2.6's login page hard-defaults
   `shouldCreateUser` to false) — then the OTP-verify step. `email` is read-only, taken
   from the invite, never typed by the invitee.

5. **Signup is refused before an account is even created when there is no valid,
   unexpired, pending invite for the email — enforced in Postgres, not just hidden in
   the UI.** A Supabase **"before user created" Auth Hook**
   (`public.check_signup_invite(event jsonb) returns jsonb`, registered in
   `supabase/config.toml`'s `[auth.hook.before_user_created]` — a section that does
   **not** exist in `config.toml` today; there is no `[auth.hook.*]` block at all, so
   this is a pure addition next to the existing `[auth]` / `[auth.email]` blocks at
   `config.toml:68-108`) rejects the signup attempt with a clear `403` unless `event`'s
   metadata carries a `token` matching a `pending`, unexpired `invites` row for that
   email, **and** the metadata's `age_affirmed` is `true`.
   **Availability is verified by running the stack, not by reading a version pin —
   there is no Supabase CLI pin in this repo** (no `supabase` entry in
   `package.json` dependencies or devDependencies; `.github/workflows/deploy.yml:37-38`
   uses `supabase/setup-cli@v3` with no `version:` input; `npx supabase` resolves
   whatever npm serves). Decided by: add the block, then
   `DBUS_SESSION_BUS_ADDRESS=/dev/null npx supabase stop &&
   DBUS_SESSION_BUS_ADDRESS=/dev/null npx supabase start` boots clean and
   `DBUS_SESSION_BUS_ADDRESS=/dev/null npx supabase --version` is recorded in the PR
   description. If the stack refuses the hook type, the fallback is documented in Dev
   Notes "If the Auth Hook is unavailable" — do not silently skip enforcement either
   way, and do not skip the DBUS prefix (see Dev Notes "Running the Supabase CLI here").

6. **`role` is never read from the request body — verified, not assumed.**
   `handle_new_user()`'s rewritten binding step (AC-7) resolves the granted role
   exclusively from `invites.role`, looked up server-side by the token in
   `raw_user_meta_data`. `grep -n "raw_user_meta_data.*role\|meta.*\.role"
   supabase/schemas/*.sql` returns **zero** hits that feed a role assignment.
   **This grep already returns zero hits today** (today's `handle_new_user()` reads only
   name fields out of `raw_user_meta_data`), so this AC is a **regression guard on the
   rewrite**, not a change to make: it is satisfied by *not* introducing a
   metadata-driven role, and it fails the moment someone does. Re-run it after Task 4.

7. **`handle_new_user()` drops the old bootstrap-first-user fallback and binds from the
   invite instead.** The `if not exists (select 1 from public.account_members) then …
   bootstrap …` branch (today's `02_functions.sql:57-70`, inside the function at
   `02_functions.sql:25-74`) is deleted outright — not merely made conditional — along
   with its now-dead `v_account_id` declaration and the header comment's
   "the FIRST user bootstraps the tenant" paragraph (`02_functions.sql:15-24`, whose
   "AD-11's full invite binding is Epic-1" is stale pre-renumbering text meaning *this*
   story). In its place: look up the `pending`, unexpired `invites` row whose `token`
   matches `new.raw_user_meta_data->>'invite_token'`; if found, insert the
   `account_members` row with that invite's `account_id`/`role`/`invited_by` and mark
   the invite `accepted` (`status = 'accepted'`, `accepted_at = now()`); if not found
   (should not happen once AC-5's hook is live, but handled defensively), create **no**
   membership at all — the caller falls through to `current_context_id()`'s existing
   fail-closed NULL, exactly as an uninvited signup does today. The `members` profile
   insert (`02_functions.sql:31-55`) is unchanged; `member_count` still drives
   `administrator`, which AC-9 deliberately keeps.

8. **`isInitialized`/`init_state` are deleted — the last piece of AD-1's "drop
   `init_state`" directive.** `SignupPage.tsx` and its `/sign-up` route are deleted
   (superseded entirely by `/accept-invite/:token` — there is no other signup entry
   point once invites gate everything); `providers/supabase/dataProvider.ts`'s
   `isInitialized()` (`:209`), `authProvider.ts`'s `getIsInitialized` (`:36-53`) /
   `IS_INITIALIZED_CACHE_KEY` (`:26`, `:87`), and every `checkAuth`/`canAccess` branch
   that reads it (`:140-156`, `:161-162`) are removed; the `init_state` view
   (`03_views.sql:6-10`) and its `anon`/`authenticated`/`service_role` grants
   (`06_grants.sql:50-52`) are dropped from the schema; the grants file's
   "Views that must be readable before sign-in (init_state and friends)" comment
   (`06_grants.sql:37`) loses its now-false clause.
   **Route registration is the manifest, not JSX**: `SignupPage` is one entry in
   `root/routeManifest.ts`'s `CUSTOM_ROUTES` (`routeManifest.ts:50-55`, `surface:
   "both"`, plus its import at `:12`) — there are no per-surface `<Route>` elements in
   `root/CRM.tsx` to delete. Decided by:
   `grep -rn "init_state\|isInitialized\|IS_INITIALIZED" supabase/schemas/ src/`
   returns **zero** hits (it returns **25** today).

9. **`is_admin()` is retired — the other half of AD-2's "retire
   `is_admin()`/`isInitialized`" directive, deferred to Epic 2 by name by story 1.2
   ("retiring `is_admin()` itself is AD-1 / Epic 2, not this story") and re-assigned to
   this story by name by story 2.1's own Dev Notes.** The SQL function
   `public.is_admin()` (`02_functions.sql:104-113` — no header comment) is deleted with
   its three grant lines (`06_grants.sql:24-26`), and the **two** `configuration`
   policies that call it (`Enable insert for admins`, `Enable update for admins` —
   `05_policies.sql:23-24`) are dropped **without replacement** — the `configuration`
   singleton (branding/config jsonb) becomes read-only to `authenticated` (its existing
   `using (true)` read policy, `05_policies.sql:22`, is unchanged) and writable only by
   `service_role`, a platform-ops runbook action exactly like this story's genesis seed.
   This is safe to do bluntly because the write path is already dead in the app:
   `dataProvider.updateConfiguration()` has **zero** UI callers (re-verified — the only
   `getConfiguration()` callers are `root/CRM.tsx:158,175` and
   `root/useConfigurationLoader.ts:17`, and `useConfigurationLoader.ts:13,24`'s
   `updateConfiguration` is `useConfigurationUpdater()` from `root/ConfigurationContext`,
   a **store setter with the same name — not the dataProvider method**; do not let that
   grep hit fool you into thinking there is a caller). So `updateConfiguration` is
   deleted from both providers (`providers/supabase/dataProvider.ts:487`,
   `providers/fakerest/dataProvider.ts:740`) along with the `resource: "configuration"`
   entry in the supabase provider's `lifeCycleCallbacks`
   (`providers/supabase/dataProvider.ts:513-…`, wired in at `:564`). The frontend's admin
   special-casing of `configuration` goes with it: the `params.resource ===
   "configuration"` branch in `providers/commons/canAccess.ts:25-28` is removed, as are
   the two `<CanAccess resource="configuration" action="edit">` wrappers (around the
   Settings nav item in `layout/Sidebar.tsx:49-51` and the Settings menu item in
   `layout/TopBar.tsx:37-39`) — Settings is a per-member surface (Personas 2.5, Invites
   2.8), and the mobile bottom nav already exposes it ungated, so the admin gate was both
   wrong and inconsistent. **Kept, deliberately:** the `members.administrator` column,
   the `"admin"`-role mapping in `authProvider.canAccess` (`authProvider.ts:169`), and
   the members-resource gating (`canAccess.ts:20-23`, `TopBar.tsx:34-36`) — they feed
   the fork's user-management list, whose replacement is Epic 3/6's `useViewerRole` work,
   not this story's. After this story:
   `select to_regproc('public.is_admin')` returns NULL, and
   `grep -rn "is_admin" supabase/schemas/ src/` returns **zero** hits (it returns **6**
   today, across `02_functions.sql`, `05_policies.sql` and `06_grants.sql`). Historical
   hits under `supabase/migrations/` are expected and out of scope — the grep is
   deliberately scoped to `supabase/schemas/ src/`.

10. **The genesis seed pattern actually works, proven by a test, not just described —
    and the two existing suites that assert the *old* bootstrap are updated in the same
    change.** A new `supabase/tests/invites.sql` + `invites.test.ts` (the `.test.ts`
    following the existing psql-runner shape: `execFileSync("psql", …)`, an
    `isolatedScript()` `begin;`-wrapped `\i`, the trailing JSON report line, and
    `bailIfDbUnreachable` from `supabase/tests/dbSuiteHelpers.ts`) covering:
    (a) a `service_role`-inserted invite with `invited_by = null` succeeds, and **any**
    direct `insert` or `update` on `invites` as `authenticated` fails — including a
    `helper`'s hand-crafted `role = 'parent_admin'` insert with a non-null `invited_by`
    (AC-2's escalation case); (b) a matching signup (simulated by inserting into
    `auth.users` with the right `raw_user_meta_data`, mirroring
    `supabase/tests/members_rename.sql:89-95` — the one existing suite that inserts
    *with* `raw_user_meta_data`) binds the correct `account_id`/`role` and flips the
    invite to `accepted`; (c) a signup with **no** matching invite creates zero
    `account_members` rows (AC-7's fallback case); (d) `create_invite()` refuses a
    `helper`/`single` caller (AC-3), refuses granting a role above the caller's own
    authority, and refuses a role/context-kind mismatch (a `shadchan`-role invite from a
    household context; a non-`shadchan`-role invite from a shadchanus context).
    **Regression, not optional:** AC-7 deletes the branch that two shipped assertions
    still test — `members_rename.sql:109-115` ("handle_new_user() bootstraps the first
    user's account_members row") and `references_entity.sql:36-40` ("the first user to
    sign up bootstraps a parent_admin membership"). Both must be rewritten in this
    story to assert the new truth (a signup with no invite gets **no** membership),
    not deleted wholesale — `references_entity.sql:46-49`'s "a later signup with no
    invite gets NO membership" already states it and survives unchanged. Decided by:
    `npm run test:unit:db` green.

11. **Toolchain green**: `make typecheck && npm run lint && make test &&
    npm run test:unit:db`, plus `node scripts/check-retired-names.mjs` and
    `node scripts/check-suppressions.mjs` (the two CI guards in
    `.github/workflows/check.yml:144,147`).

## Tasks / Subtasks

- [x] **Task 1 — `invites` table + RLS** (AC: 1, 2)
  - [x] `01_tables.sql`: add the `invites` table per AC-1 — columns inline, the two
        FKs in the file's `alter table … add constraint …_fkey` section, and
        `create index invites_account_id_idx …` in the indexes section. Do **not**
        write inline `references` clauses; nothing else in this file does.
  - [x] `05_policies.sql`: `alter table public.invites enable row level security;`
        (no `force` — AC-2) + the **select-only** account-scoped policy per AC-2 —
        deliberately no insert/update/delete policy for `authenticated` (verify by
        writing the negative tests in Task 9 before declaring this done, not after).
  - [x] `06_grants.sql`: `revoke all on table public.invites from anon;
        grant select on table public.invites to authenticated;
        grant all on table public.invites to service_role;` — DML is withheld at the
        grant level (AC-2's rationale); the sequence `invites_id_seq` gets no
        `authenticated` grant either, since `authenticated` never inserts directly.
        (Note the file's existing sequence grants hand `anon` `all`; do **not** copy
        that pattern here.)

- [x] **Task 2 — `role_authority()` + `create_invite()`** (AC: 3)
  - [x] `02_functions.sql`: `role_authority(role text) returns int` (`IMMUTABLE`), then
        `create_invite()` per AC-3.
  - [x] `06_grants.sql`: `revoke all on function … from public, anon;` for both,
        then `execute` to `authenticated` — the file's standard revoke-then-grant
        pattern used by every MyShadchan-era function (`06_grants.sql:197-238,
        304-335`); PUBLIC gets EXECUTE by default otherwise.

- [x] **Task 3 — `get_invite_preview()`** (AC: 4)
  - [x] `02_functions.sql`: `SECURITY DEFINER`, `STABLE`, `SET search_path = ''`,
        returns only the five fields named in AC-4 — never the inviting account's own
        data, never the invite's `invited_by`/`id`/`token`. This is deliberately
        **anonymously readable** (it is how an unauthenticated invitee previews their
        invite before signing up), so grant `execute` to `anon` as well as
        `authenticated` — the one new anon-callable surface this story adds, and it is
        narrow by construction (AC-4's field list). Note this is a deliberate, scoped
        exception to AD-1's "the only anon-readable relation is the published-listing
        snapshot": it is a function returning five non-domain fields for a caller who
        already holds the token, not a relation. Say so in the function's comment.

- [x] **Task 4 — `handle_new_user()` rewrite** (AC: 6, 7)
  - [x] `02_functions.sql:25-74`: delete the bootstrap branch (`:57-70`) and the
        `v_account_id` declare; add the invite-lookup-and-bind branch per AC-7. Keep the
        `members` profile insert (`:31-55`) unchanged — that half is not this story's.
  - [x] Rewrite the stale header comment (`:15-24`) so it describes invite binding
        rather than "the FIRST user bootstraps the tenant"; its "(AD-11's full invite
        binding is Epic-1)" aside is pre-renumbering text for *this* story.
  - [x] Add the 18+ affirmation check here as defense-in-depth (see Dev Notes "Two
        gates, one authoritative") **only if** AC-5's hook cannot itself block user
        creation in this Supabase version — do not build both as independent, divergent
        gates when one suffices.
  - [x] Re-run AC-6's grep; confirm still zero.

- [x] **Task 5 — The signup-gating Auth Hook** (AC: 5)
  - [x] `02_functions.sql`: `check_signup_invite(event jsonb) returns jsonb` per AC-5.
  - [x] `supabase/config.toml`: add the `[auth.hook.before_user_created]` block (no
        `[auth.hook.*]` section exists today — place it after `[auth.email…]`, before
        `[auth.sms]` at `:109`).
  - [x] `06_grants.sql`: `revoke all on function public.check_signup_invite(jsonb)
        from public, anon, authenticated;` then grant `execute` to
        `supabase_auth_admin` (the role GoTrue hooks run as — confirm the exact role
        name against the running stack, `\du` in psql, before granting; there is no
        version pin to read it off, see AC-5).
  - [x] Record `npx supabase --version` (DBUS-prefixed) in the PR description as the
        evidence AC-5 asks for.

- [x] **Task 6 — Delete `isInitialized`/`init_state`** (AC: 8)
  - [x] `03_views.sql`: drop `init_state` (`:6-10`) and its leading comment.
  - [x] `06_grants.sql`: drop the three `init_state` grant lines (`:50-52`) and fix the
        now-false "(init_state and friends)" clause in the table-grants comment (`:37`).
  - [x] `git rm src/components/atomic-crm/login/SignupPage.tsx`; remove its
        **single** `routeManifest.ts` entry (`:50-55`) and its import (`:12`). There is
        no `<Route>` in `root/CRM.tsx` to touch — `renderCustomRoutes` maps the
        manifest for both surfaces.
  - [x] `git rm src/components/atomic-crm/login/StartPage.tsx` — its whole purpose is
        the `isInitialized` branch (route to `/sign-up` for an uninitialized install),
        which no longer exists. Change `root/CRM.tsx`'s `loginPage={StartPage}`
        (`CRM.tsx:204`) to `loginPage={LoginPage}` and swap the import (`CRM.tsx:38`).
  - [x] `git rm src/components/atomic-crm/login/ConfirmationRequired.tsx` and its
        **single** `routeManifest.ts` entry (`:56-61`) and import (`:11`): its only
        navigator was `SignupPage.tsx:64`'s email-confirmation redirect, and the OTP
        flow has no confirmation-link step — the typed-back code is the confirmation.
  - [x] Remove `authProvider.ts`'s `#/sign-up` `checkAuth` special-case (`:140-146`)
        **and** the `getIsInitialized()`-driven `redirectTo: "/sign-up"` throw
        (`:148-156`) — the sibling `/set-password`/`/forgot-password` cases went with
        2.6, so `checkAuth` reduces to `return baseAuthProvider.checkAuth(params)`.
  - [x] `providers/supabase/dataProvider.ts`: remove `isInitialized()` (`:209`) and
        `signUp()` (`:117-…`, the old password-signup path — dead once `SignupPage.tsx`
        is gone; confirm no other caller before deleting). Remove `SignUpData` from
        `src/components/atomic-crm/types.ts:3-8` if nothing else references it.
  - [x] `providers/supabase/authProvider.ts`: remove `getIsInitialized` (`:36-53`),
        `IS_INITIALIZED_CACHE_KEY` (`:26`, and its `clearCache` removal at `:87`), and
        the `canAccess` branch that calls it (`:161-162`).
  - [x] `providers/fakerest/dataProvider.ts`: remove the matching `isInitialized`
        (`:543`) and `signUp` (`:488`) emulations; confirm the FakeRest demo boot path
        (which never needed `isInitialized` to gate anything meaningful) still starts
        (AD-10) via `npm run dev:demo`.
  - [x] Run AC-8's grep; it must return zero (25 hits today).

- [x] **Task 7 — Retire `is_admin()` and the `configuration` write path** (AC: 9)
  - [x] `02_functions.sql`: delete `is_admin()` (`:104-113`).
        `05_policies.sql`: drop `Enable insert for admins` / `Enable update for
        admins` on `public.configuration` (`:23-24`); keep `Enable read for
        authenticated` (`:22`) and update the section's "(admin-only for writes)"
        comment (`:21`) to say service-role-only.
        `06_grants.sql`: delete `is_admin()`'s three grant lines (`:24-26`); leave
        `configuration`'s table grants (`:46-47`) alone (writes are stopped by the
        absence of any insert/update policy, and `service_role` bypasses RLS), but
        correct the "written by admins only (enforced by RLS)" comment (`:44-45`).
  - [x] `providers/supabase/dataProvider.ts`: delete `updateConfiguration()` (`:487`)
        and the `resource: "configuration"` lifecycle-callback block (`:513-…`);
        `providers/fakerest/dataProvider.ts`: delete its `updateConfiguration`
        emulation (`:740`). Keep `getConfiguration()` in both (`:481` / `:734`) — it has
        three live callers.
  - [x] `providers/commons/canAccess.ts`: remove the `configuration` branch (`:25-28`);
        keep the `members` branch (`:20-23`).
        `layout/Sidebar.tsx:49-51` and `layout/TopBar.tsx:37-39`: unwrap the two
        `<CanAccess resource="configuration" action="edit">` wrappers so the Settings
        entry points render for every member (drop the now-unused `CanAccess` import in
        `Sidebar.tsx:1`; `TopBar.tsx:1` still needs it for the `members` wrapper).
  - [x] Run AC-9's `to_regproc` check and grep; both must come back empty.

- [x] **Task 8 — `InviteAcceptance.tsx` rewrite** (AC: 4)
  - [x] Rewrite per AC-4: fetch the preview via `get_invite_preview`, render
        `AgeAffirmation` (`compact` mode, per its existing prop), then Story 2.6's
        two-step OTP form with `email` locked to the invite's. Delete the file's
        current `onAccept(password)` prop contract and its "no invite-token
        table/edge-function yet" docstring; fix `AgeAffirmation.tsx:31-33`'s comment,
        which points at `FirstRunSetup` as its embedder even though nothing imports it.
  - [x] Add the route as a **new `CUSTOM_ROUTES` entry** in `root/routeManifest.ts`
        (`path: "/accept-invite/:token"`, `surface: "both"`, `chrome: "bare"`), taking
        the slot the deleted `SignupPage` entry vacates. `routeManifest.test.ts`'s
        "returns no violations for the real manifest" must stay green.
  - [x] Add the flow's copy under `crm.auth.invite_*` / `crm.auth.age_affirmation.*` to
        **both** message catalogues (`providers/commons/englishCrmMessages.ts` and
        `frenchCrmMessages.ts`) — neither carries these keys today (verified: zero
        hits), so both components currently rely on inline `_:` defaults, which is not
        the house convention (cf. 2.8's `crm.settings.invites_*`).
  - [x] An expired or already-`accepted`/`revoked` invite renders a clear, specific
        message ("This invite has expired — ask the person who invited you for a new
        one"), never a generic error. Note `get_invite_preview` returns no inviter
        name, so do not promise one in the copy.

- [x] **Task 9 — Tests and CI guards** (AC: 10, 11)
  - [x] `supabase/tests/invites.sql` + `invites.test.ts` per AC-10, following
        `members_rename.test.ts`'s runner shape (`bailIfDbUnreachable`, `isolatedScript`,
        JSON report line) and `members_rename.sql:89-95`'s "insert into `auth.users`
        with `raw_user_meta_data`, assert the trigger's effect" pattern for case (b).
  - [x] Update `supabase/tests/members_rename.sql:109-115` and
        `supabase/tests/references_entity.sql:36-40` — both currently assert the
        first-user bootstrap AC-7 deletes. Rewrite, don't delete (AC-10).
  - [x] Consider adding an `is_admin|init_state|isInitialized` pattern to
        `scripts/retired-names.json` so AC-8/AC-9's "zero hits" becomes a standing CI
        guard rather than a one-time grep — the same AD-23 mechanism Epic 1's renames
        use. If added, the new pattern's `exampleFragments` must not spell the retired
        identifier in `check-retired-names.mjs` itself (see that file's header), and
        `node scripts/check-retired-names.test.mjs` must stay green.
  - [x] `make typecheck && npm run lint && make test && npm run test:unit:db &&
        node scripts/check-retired-names.mjs && node scripts/check-suppressions.mjs`.

## Dev Notes

### Running the Supabase CLI here

Every `npx supabase …` invocation in this repo must be prefixed with
`DBUS_SESSION_BUS_ADDRESS=/dev/null` — without it the CLI blocks on the desktop keyring
and hangs indefinitely, which presents as a Docker failure but is not one. This matters
for Task 5, which is the one task in this story that has to boot the stack rather than
just diff SQL.

### Why the token — not just the email — drives the binding

`handle_new_user()` matches by a `token` carried in `raw_user_meta_data`, not by email
alone, specifically to stay correct when one email address holds more than one pending
invite (invited into two different households, or re-invited after an earlier invite
expired). The token arrives in metadata because Story 2.6's `signInWithOtp` call already
supports an `options.data` payload (2.6 AC-2 forwards `params.meta` → `options.data`) —
`InviteAcceptance.tsx` threads `invite_token` and `age_affirmed` through that same
mechanism, not a new one.

### Two gates, one authoritative

AC-5's "before user created" hook and AC-7's `handle_new_user()` rewrite both *could*
enforce the invite/age-affirmation requirement, but only one should be the actual gate —
having both check independently risks them silently disagreeing over time. **The hook
is authoritative** (it runs first, and can outright refuse account creation with a
clear error the frontend can display before any row exists). `handle_new_user()` only
performs the *binding* once creation has already been allowed; Task 4's defensive
duplicate check is a fallback for the one scenario where the hook type turns out to be
unavailable in this Supabase version (Task 4/AC-5 both call this out explicitly so it is
not silently built twice as normal practice).

### If the Auth Hook is unavailable — verify by running, not by reading a pin

Supabase's "before user created" Postgres hook is a newer GoTrue capability. **This repo
pins no Supabase CLI version** (nothing in `package.json`; `supabase/setup-cli@v3` with
no `version:` in `.github/workflows/deploy.yml:37-38`), so ARCHITECTURE-SPINE.md's Stack
row (`@supabase/supabase-js` 2.110.8 / CLI 2.109.1) is a *seed intention*, not something
the tree enforces — do not treat it as the answer. Verify empirically per AC-5: add the
`[auth.hook.before_user_created]` block and boot the stack. If the hook is genuinely
unavailable: the fallback is `handle_new_user()` raising an exception when no matching
invite/affirmation is found, which rolls back the whole `auth.users` insert
transaction — a real, working data-level enforcement, just with a less friendly failure
mode (a raw Postgres error surfaces through GoTrue rather than a clean 403). Either way,
"signup without a valid invite is refused" must be provably true by Task 9's tests — the
tests do not change based on which mechanism enforces it.

### Why `self_manager` is never invited (AC-1)

`self_manager` means "a single who runs their own process, with no parent involved" — it
is a role a person arrives at through `add_persona('single')` finding no existing
household (Story 2.2's own logic), never a role a *second* person is invited into. An
invite that granted `self_manager` to an invitee would create a contradiction: a
household with an inviting `parent_admin` already present, and a "no parent involved"
role sitting inside it. Excluded from `invites.role`'s check constraint for this reason,
not by oversight. Note `account_members_role_check` today
(`01_tables.sql:139-141`) is `('parent_admin', 'helper', 'self_manager', 'shadchan')` —
Story 2.2 adds `single`, which AC-1's invitable list assumes.

### Do not touch `07_storage.sql`

`supabase/schemas/07_storage.sql` carries **three** account-scoped policies on
`storage.objects` (select / insert / delete on the `attachments` bucket, keyed on the
`{account_id}/` path prefix) that call the context resolver. They live in a different
schema file from every other policy in the repo and are easy to miss in a
policies-wide sweep — but they are **not this story's**: nothing here renames the
resolver (Story 2.1 does that, and owns updating them) and nothing here adds a storage
policy. Listed only so a reviewer sweeping "every policy" does not mistake their absence
from this diff for an omission.

### Verified current state (re-verified against the post-Epic-1 tree)

- `handle_new_user()` — `02_functions.sql:25-74`; the bootstrap branch this story
  deletes is `:57-70`; its header comment `:15-24` already flags itself as a
  placeholder ("Membership model here is deliberately minimal and invite-free (AD-11's
  full invite binding is Epic-1) … see nothing until the invite flow grants them one" —
  the "Epic-1" there is stale pre-renumbering text; the invite flow is this story).
- `login/InviteAcceptance.tsx` (**153** lines) and `login/AgeAffirmation.tsx` (**118**
  lines): both self-documented as UI shells with no backend wiring, and both currently
  imported by **nothing** — no route, no parent component. This story is that wiring
  *and* their first use.
- `login/SignupPage.tsx` (238 lines), gated by `isInitialized` — deleted whole (AC-8).
  Story 2.6 has already stripped its password/Google/SSO UI by the time this story
  runs (2.6 Task 6 explicitly leaves the gating logic here), so expect a
  half-migrated file, not the 238-line original.
- `root/routeManifest.ts` — the single registration point (story 1.5). `SignupPage`
  at `:50-55`, `ConfirmationRequired` at `:56-61`, both `surface: "both"`.
  `root/CRM.tsx:47-57` is the only place `<Route>`/`<Resource>` elements are written,
  and it writes them by mapping the manifest. `routeManifest.test.ts` asserts the real
  manifest yields zero violations — it will exercise the new `/accept-invite/:token`
  entry for free.
- `03_views.sql:6-10` (`init_state`) and `06_grants.sql:50-52` (its three grants) —
  the last surviving pre-Epic-1 `security_invoker = off` definer view, explicitly
  flagged by AD-1 ("no definer views — drop `init_state`") and by 1.1/1.2's own Dev
  Notes as "Epic 2's to delete." Every other view in `03_views.sql` is
  `security_invoker = on`.
- `is_admin()` at `02_functions.sql:104-113`; its policies at `05_policies.sql:23-24`;
  its grants at `06_grants.sql:24-26`. Six hits total under `supabase/schemas/ src/`.
- `supabase/functions/users/index.ts:127-239` (`inviteUser`, calling
  `auth.admin.inviteUserByEmail` at `:215`, dispatched from `:362`) — the fork's
  password-based admin-invite flow. **Out of scope for this story, retired by Story
  2.8** (whose epic title — invites as the *one* membership mechanism — is exactly the
  rule it violates). Note it is doubly dead after this story anyway: its emailed link
  lands on a route 2.6 deleted, and its user creation is refused by AC-5's hook (no
  `invite_token` in metadata) — 2.8 deletes the corpse.
- `supabase/config.toml` has **no** `[auth.hook.*]` section of any kind today
  (`[auth]` `:68`, `[auth.oauth_server]` `:87`, `[auth.email]` `:92`, `[auth.sms]`
  `:109`).
- The E8 onboarding surfaces added after this story was drafted —
  `login/FirstRunSetup.tsx`, `login/OnboardingChoice.tsx`, `root/OnboardingGate.tsx` —
  are **post-login** (mounted inside `layout/Layout.tsx` / `MobileLayout.tsx`), so
  deleting the signup entry point does not touch them. They are not an alternate
  signup path.

### Security posture

Two new `SECURITY DEFINER` functions (`get_invite_preview`, deliberately
`anon`-callable — the one new anon surface this story adds — and `create_invite`,
the sole authenticated write path onto a table with no client DML grant) and a new
Auth Hook make this an unambiguous `.claude/rules/security-triggers.md` case.
Specifically verify: `get_invite_preview` returns *only* the five named fields (never
`invited_by`, `token` itself, or any household data); `create_invite` performs every
check in AC-3 itself (as a definer function it is the entire write gate — RLS no
longer backstops it); the Auth Hook function is not callable by
`anon`/`authenticated` directly (only by the GoTrue hook-invoking role); and AC-9's
policy drop leaves `configuration` with a read policy and **no** write policy rather
than a permissive one.

### Testing standards

New `supabase/tests/invites.sql` + `invites.test.ts`, same shape as the existing four
suites (`billing_entitlement`, `members_rename`, `references_entity`, `shidduch_catch`),
all of which shell out to psql and share `dbSuiteHelpers.ts`'s `bailIfDbUnreachable`.
`.claude/rules/security-triggers.md`'s negative-test requirement covers AC-2 (the
`authenticated`-cannot-write case, with and without `invited_by`) and AC-10 case (c)
(no invite, no membership). Two **existing** assertions must be rewritten, not left to
fail — see AC-10.

### Project Structure Notes

Schema: `01_tables.sql`, `02_functions.sql`, `03_views.sql`, `05_policies.sql`,
`06_grants.sql`, `supabase/config.toml`. (Not `07_storage.sql` — see above.)
Frontend: `login/InviteAcceptance.tsx` (rewritten), `login/AgeAffirmation.tsx` (wired,
not rewritten), `root/routeManifest.ts` (route swap), `root/CRM.tsx` (`loginPage` swap
+ import), `providers/{supabase,fakerest}/dataProvider.ts`,
`providers/supabase/authProvider.ts`, `providers/commons/canAccess.ts`,
`providers/commons/{english,french}CrmMessages.ts`, `layout/Sidebar.tsx`,
`layout/TopBar.tsx` (AC-9's unwraps), `types.ts` (`SignUpData`).
Tests: new `supabase/tests/invites.sql` + `invites.test.ts`; edited
`supabase/tests/members_rename.sql`, `supabase/tests/references_entity.sql`;
optionally `scripts/retired-names.json`.
Deleted: `login/SignupPage.tsx`, `login/StartPage.tsx`, `login/ConfirmationRequired.tsx`.

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story-2.7] — the story's own AC
  text, verbatim.
- [Source: _bmad-output/planning-artifacts/architecture/architecture-myshadchan-2026-07-21/ARCHITECTURE-SPINE.md#AD-11]
  — "New users join **only by a verified invite token** (not email match); the invite
  server-path **binds the row to the inviter's active context** and authorizes `role ≤
  inviter authority` (no `role` mass-assignment …). **18+ affirmation** … **Invites are
  the one mechanism** (FR119)."
- [Source: …/ARCHITECTURE-SPINE.md#AD-1] — "Views are `security_invoker = on`; **no
  definer views** (drop `init_state`)."
- [Source: …/ARCHITECTURE-SPINE.md#AD-2] — "Retire `is_admin()`/`isInitialized`."
- [Source: _bmad-output/implementation-artifacts/1-2-rename-sales-to-members.md] —
  "retiring `is_admin()` itself is AD-1 / Epic 2, not this story."
- [Source: _bmad-output/implementation-artifacts/2-1-context-aware-authorisation.md] —
  re-assigns **both** halves (`isInitialized`/`init_state` and
  `is_admin()`/`configuration`) to this story by name, and records why no story owns
  the repo-wide `FORCE ROW LEVEL SECURITY` pass (AC-2's rationale for not introducing
  it here alone).
- [Source: _bmad-output/implementation-artifacts/2-6-passwordless-sign-in.md] — the
  `requestOtp`/`verifyOtp`/`allowSignup`/`meta` seam AC-4 calls, and the statement that
  `StartPage` "survives this story and is deleted by 2.7 together with `isInitialized`."

## Dev Agent Record

### Agent Model Used

Claude Opus 5 (claude-opus-4-6), via the bmad-dev-story workflow.

### Debug Log References

- `DBUS_SESSION_BUS_ADDRESS=/dev/null npx supabase --version` → **2.109.1** (recorded per AC-5's evidence requirement; no CLI version is pinned in this repo).
- Empirically verified the `before_user_created` Auth Hook contract against the running local stack (extracted the GoTrue binary from the running `supabase_auth_*` container and probed it directly with hand-written functions, since neither the Supabase docs nor this repo pin a version to read the contract off):
  - GoTrue calls the hook **only** for the self-serve `/otp` and `/signup` endpoints — **never** for a service-role `auth.admin.createUser()` call (confirmed via the auth container's structured logs: the `/admin/users` request carries no `run_hook` log line, the `/otp` request does).
  - The `event` payload's metadata lives at `event->'user'->'user_metadata'`, **not** `event->'user'->'raw_user_meta_data'` — a different key path than `handle_new_user()`'s own trigger, which reads the real Postgres column.
  - Allow: return `'{}'::jsonb`. Refuse: return `jsonb_build_object('error', jsonb_build_object('http_code', 403, 'message', '...'))` — GoTrue surfaces that exact status/message to the client (`AuthApiError`, verified end-to-end via a real `signInWithOtp({shouldCreateUser:true})` call).
  - End-to-end proof: seeded a real household + `create_invite()`-minted invite, called `signInWithOtp` with the matching token/email/`age_affirmed:true`, confirmed the hook allowed it and `handle_new_user()` bound the correct `account_id`/`role`/`invited_by` and flipped the invite to `accepted`.
- **`supabase db diff` silently dropped GRANT/REVOKE statements for every brand-new object in this migration** (functions AND, newly observed, a table and its sequence) — matching the landmine's warning but a wider blast radius than the precedent (Story 2.1's fix only needed to hand-add function grants). Caught by applying the generated migration to the real local DB and directly querying `has_table_privilege()`/`pg_class.relacl`/`has_function_privilege()` rather than trusting the diff:
  - `role_authority()`, `create_invite()`, `get_invite_preview()`, `check_signup_invite()`: zero GRANT/REVOKE statements were emitted at all; hand-added, mirroring the `member_state` precedent in `20260727223658_context_aware_authorisation.sql`.
  - `public.invites` (table): the diff emitted narrow `grant select ... to authenticated` with **no accompanying revoke**. Applied as-is, `authenticated` ended up holding `TRUNCATE`/`REFERENCES`/`TRIGGER`/`MAINTAIN` on `invites` (bypasses RLS entirely) via the schema's ambient `alter default privileges ... grant all on tables to authenticated` — a real, exploitable gap, not a cosmetic one. Root-caused to my own schema-file bug first (I'd written `revoke all on table public.invites from anon;`, omitting `authenticated` — 06_grants.sql's own house style always revokes from both), then found the migration generator doesn't correctly account for the ambient default-privilege grant when diffing a table that doesn't exist in the "current" side at all. Fixed both the schema file and hand-added the missing `revoke all on table "public"."invites" from "anon", "authenticated";` to the migration.
  - `public.invites_id_seq`: same omission; hand-added its revoke/grant too.
  - Migration re-verified afterward: `db diff` against the fully-migrated local DB reports "No schema changes found" — the migration file now reproduces exactly what re-running the schema files from scratch produces.
- `supabase/tests/invites.sql` run directly via `psql` (37 checks, all green) before wiring the vitest runner; `members_rename.sql` and `references_entity.sql` re-run the same way after their rewrites (10 and 100 checks respectively, all green) to confirm the AC-7 fallback didn't collateral-damage an unrelated assertion (it did, once: `members_rename.sql`'s `set_member_id_default()` task-insert relied on the deleted bootstrap giving the test user a membership — fixed by provisioning one explicitly in the fixture, not by weakening the assertion).

### Completion Notes List

- All 11 ACs implemented and verified (schema empirically end-to-end against the running local stack; frontend via `make typecheck`/`make lint`/`make test` + a new component test; DB via `npm run test:unit:db`).
- **AC-5 / Task 4's conditional defense-in-depth**: the Auth Hook works and is fully authoritative (verified empirically, see Debug Log), so `handle_new_user()` deliberately does **not** duplicate the 18+ affirmation check — only the invite-token binding, per "Two gates, one authoritative" in this story's own Dev Notes. Building a second, independent age check would have been the exact divergence risk that section warns about.
- **Task 9's optional retired-names.json entry**: not added. `invites.sql` already carries two standing, DB-level regression guards (`to_regproc('public.is_admin') is null`, `to_regclass('public.init_state') is null`) that are checked on every `npm run test:unit:db` run — a stronger, more specific guard than a text-pattern entry that risks its own false positives (e.g. a future comment mentioning either retired name, which this story's own drafting hit twice — see below).
- Writing this story's own comments twice tripped AC-9's literal grep (a comment saying "is_admin() retired" contains the substring being checked for zero hits) — reworded both to describe the retirement without spelling the identifier, confirmed `grep -rn "is_admin" supabase/schemas/ src/` → 0 afterward, and confirmed the reword introduced no phantom `db diff`.
- **E2E landmine (`createSingle` no longer finds an `account_members` row) confirmed exactly as flagged**: fixed by having `createSingle()` provision its own account + membership directly via the service-role client (the same "genesis seed" shape this story's Dev Notes describe), rather than looking one up. `createMember()` itself needed no change — `admin.createUser()` is not gated by the hook (see Debug Log).
- **E2E landmine (does the hook fire for `auth.admin.createUser`) resolved: no.** Verified against the dev stack; the same GoTrue binary/behavior is wired into the e2e stack via the identical `[auth.hook.before_user_created]` block added to `config.e2e.toml`, so this generalizes without needing to spin up the full e2e stack separately (`e2e-conventions` also explicitly discourages running Playwright locally — CI runs it).
- **UI test coverage beyond the story's own AC-11 proof list**: `InviteAcceptance.test.tsx` (8 cases: preview render, no-editable-email, affirmation gates the OTP request, `allowSignup`/`meta` payload shape, OTP verify, and all three "unavailable" states) and a new `e2e/invite-acceptance.spec.ts` were added because `.claude/skills/e2e-conventions` requires an e2e spec for any UI/form change and this story ships an entirely new user-facing page; AC-11 itself only lists `make typecheck && npm run lint && make test && npm run test:unit:db` (unlike 2.6's proof, which explicitly ran `make test-e2e-ci`) — the new e2e spec was written to the same conventions as `pipeline.spec.ts`/`fixtures.ts` but, per that skill's own instruction, was not run locally; CI runs it.
- Dead i18n keys that existed only for the deleted `SignupPage.tsx`/`ConfirmationRequired.tsx` (`crm.auth.signup.*`, `crm.auth.first_name`, `crm.auth.last_name`, `crm.auth.welcome_title`, `crm.auth.confirmation_required`) were removed from both catalogues in the same change (Greenfield/NFR-14), replaced with `crm.auth.invite_*` / `crm.auth.age_affirmation.*` in both English and French.
- `SidebarNavItem`'s wrapper component collapsed to nothing once its only branch (the `configuration` `CanAccess` gate) was removed — deleted the now-pointless indirection rather than leaving a pass-through component (`Sidebar.tsx`).
- Genesis-seed runbook (two `service_role` inserts — one `accounts` row, one `invites` row with `invited_by = null`) is documented inline in `01_tables.sql`'s `invites` comment and exercised end-to-end by `invites.sql`'s first assertion; no separate runbook doc was created (out of this story's file scope).
- Full toolchain green: `make typecheck`, `make lint` (eslint + prettier), `npm run test` (801/801), `npm run test:unit:db` (310/310 across 6 suites including the new `invites.test.ts`'s 37 checks), `node scripts/check-retired-names.mjs`, `node scripts/check-suppressions.mjs`.

### File List

**Schema:**
- `supabase/schemas/01_tables.sql` — added `invites` table, its two FKs, its index.
- `supabase/schemas/02_functions.sql` — added `role_authority()`, `create_invite()`, `get_invite_preview()`, `check_signup_invite()`; rewrote `handle_new_user()`'s membership branch; deleted `is_admin()`.
- `supabase/schemas/03_views.sql` — dropped `init_state`.
- `supabase/schemas/05_policies.sql` — added the `invites` select policy + its RLS-enable line; dropped `configuration`'s two admin-write policies; reworded two stale comments (the `is_admin()`-retirement note and the `single`-role interactions-visibility note).
- `supabase/schemas/06_grants.sql` — added `invites` table/sequence grants and the four new functions' grants; deleted `is_admin()`'s grant block and `init_state`'s three grant lines; reworded two comments.
- `supabase/config.toml` / `supabase/config.e2e.toml` — added `[auth.hook.before_user_created]`.
- `supabase/migrations/20260728050724_invite_only_signup.sql` — generated migration, hand-corrected for `db diff`'s dropped GRANT/REVOKE statements (see Debug Log).

**Frontend:**
- `src/components/atomic-crm/login/InviteAcceptance.tsx` — rewritten (was a UI-only shell with no backend wiring; now the invitee's real signup path).
- `src/components/atomic-crm/login/InviteAcceptance.test.tsx` — new.
- `src/components/atomic-crm/login/AgeAffirmation.tsx` — two stale doc comments fixed (no behavior change).
- `src/components/atomic-crm/login/SignupPage.tsx` — deleted.
- `src/components/atomic-crm/login/StartPage.tsx` — deleted.
- `src/components/atomic-crm/login/ConfirmationRequired.tsx` — deleted.
- `src/components/atomic-crm/login/PasswordToggleButton.tsx` — deleted (its only importer, `SignupPage.tsx`, is gone).
- `src/components/atomic-crm/root/routeManifest.ts` — swapped the `SignupPage`/`ConfirmationRequired` entries for one `InviteAcceptance` entry.
- `src/components/atomic-crm/root/CRM.tsx` — `loginPage={StartPage}` → `loginPage={LoginPage}`.
- `src/components/atomic-crm/providers/supabase/authProvider.ts` — removed `getIsInitialized`/`IS_INITIALIZED_CACHE_KEY` and their `checkAuth`/`canAccess` branches.
- `src/components/atomic-crm/providers/supabase/dataProvider.ts` — removed `isInitialized()`, `signUp()`, `updateConfiguration()`, the `configuration` lifecycle callback, and `processConfigLogo`; added `getInvitePreview()`.
- `src/components/atomic-crm/providers/fakerest/dataProvider.ts` — mirrored the same removals; added a `getInvitePreview()` stub that always resolves "not found" (no `invites` collection exists in the demo world until Story 2.8).
- `src/components/atomic-crm/providers/commons/canAccess.ts` — removed the `configuration` branch.
- `src/components/atomic-crm/providers/commons/englishCrmMessages.ts` / `frenchCrmMessages.ts` — removed dead `crm.auth.signup.*`/`first_name`/`last_name`/`welcome_title`/`confirmation_required` keys; added `crm.auth.invite_*` / `crm.auth.age_affirmation.*`.
- `src/components/atomic-crm/layout/Sidebar.tsx` — unwrapped the `configuration` `CanAccess` gate; collapsed the now-pointless `SidebarNavItem` wrapper.
- `src/components/atomic-crm/layout/TopBar.tsx` — unwrapped the `configuration` `CanAccess` gate around the Settings menu item (kept the `members` one).
- `src/components/atomic-crm/types.ts` — removed `SignUpData`; added `InvitePreview`.

**Tests:**
- `supabase/tests/invites.sql` — new (37 checks).
- `supabase/tests/invites.test.ts` — new.
- `supabase/tests/members_rename.sql` — rewrote the first-user-bootstrap assertion; added an explicit household/membership arrange step the deleted bootstrap used to provide.
- `supabase/tests/references_entity.sql` — rewrote the first-user-bootstrap assertion; left the already-correct "later signup, no invite" assertion untouched.
- `e2e/fixtures.ts` — `createSingle()` now provisions its own account + membership; exported `fetchOtpCode`; added a `createInvite` fixture; updated `TABLES` comment.
- `e2e/invite-acceptance.spec.ts` — new.
