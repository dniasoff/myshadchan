name = "myshadchan-ai"
main = "index.ts"
compatibility_date = "2026-07-21"
compatibility_flags = ["nodejs_compat"]

# Secrets (set via `wrangler secret put <NAME> --config workers/ai/wrangler.toml`,
# or the CI job in .github/workflows/deploy.yml): SUPABASE_URL,
# SUPABASE_SERVICE_ROLE_KEY, plus the AI Gateway account/token once the research
# assistant (AD-8, E10) lands.
