# Wave 7 Close — 2026-08-09

**Commit:** b8539ce
**Story:** 13.1 — Grant lifecycle (propose/accept/sever/re-grant child access)

## Excursions (post-wave)
- **UNOWNED:** `registry.json` — auto-regenerated by pre-commit hook (expected)
- **UNOWNED:** `scripts/retired-names.json` — added exemptions for new grant-related files (ChildGrant uses "child" which matches retired patterns)
- **UNOWNED:** `src/components/atomic-crm/types.ts` — added ChildGrant and ChildGrantPreview types
- **UNCLAIMED:** `supabase/tests/migration-data-safety/declared-moves.sql` — declared but not needed (no column moves)
- **UNCLAIMED:** `src/components/atomic-crm/members/**` — declared but not needed (grant UI is in singles/)

## Cross-reconciliation
Single-story wave. Grant lifecycle implemented with database functions mirroring `connection_invites` pattern: `create_child_grant`, `revoke_child_grant`, `preview_child_grant`, `accept_child_grant`, `sever_child_grant`, `regrant_child_grant`. RLS policies added for proposer/grantee visibility. Migration safety PASSED on stack 3 (fixture fixes for founding_member_id, analytics_events empty_by_design, and account_members trigger guards).

## Gates
- ✅ typecheck, lint, build, all 5 guard scripts, migration-safety
- ⚠️ `make test` — pre-existing failures: 4 cron tests (missing `ctx.waitUntil` mock), 14 billing tests (missing `ExecutionContext` mock), browser tests (vitest dev server connection) — belong to Stories 12.2/12.4

## Stacks released
- STACK_ID=3 released via `make stop-supabase-e2e STACK_ID=3`