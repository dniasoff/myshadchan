# Wave 8 Close — 2026-08-09

**Commit:** f30c8ef
**Story:** 13.2 — When someone leaves the household (archive, undo, past toggle)

## Excursions (post-wave)
- **UNOWNED:** `_bmad-output/implementation-artifacts/2-5-persona-lifecycle-changes.md` — amended as required by E13-D14 default (b)
- **UNOWNED:** `registry.json` — auto-regenerated by pre-commit hook (expected)
- **UNOWNED:** `src/components/atomic-crm/misc/EntityList.tsx` — added `filterDefaultValues` support for singles roster default filter
- **UNOWNED:** `supabase/schemas/06_grants.sql` — added grants for `remove_persona_admin`/`restore_persona_admin` functions
- **UNCLAIMED:** `supabase/schemas/05_policies.sql` — declared but not needed (RLS handled by SECURITY DEFINER functions)
- **UNCLAIMED:** `supabase/tests/migration-data-safety/fixture.sql` — declared but not needed (no new seed rows needed)
- **UNCLAIMED:** `supabase/tests/migration-data-safety/declared-moves.sql` — declared but not needed (no column moves)

## Cross-reconciliation
Single-story wave. Persona removal extended from self-only to admin-capable with `remove_persona_admin()` / `restore_persona_admin()` SECURITY DEFINER functions. Singles roster now defaults to hiding archived people with a "Past members" toggle (reverses Story 2.5 AC-8 per E13-D14 default b). `2-5-persona-lifecycle-changes.md` and `SingleCard.tsx` amended in same dispatch per parallel-ownership.md. MemberList gets removal action with neutral confirmation dialog (no reason field per hard constraint). Migration safety PASSED on stack 4.

## Gates
- ✅ typecheck, lint, build, all 5 guard scripts, migration-safety
- ⚠️ `make test` — pre-existing failures: 4 cron tests (missing `ctx.waitUntil` mock), 14 billing tests (missing `ExecutionContext` mock), browser tests (vitest dev server connection) — belong to Stories 12.2/12.4

## Stacks released
- STACK_ID=4 released via `make stop-supabase-e2e STACK_ID=4`